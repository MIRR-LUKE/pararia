##############################################
# Local dev template (rename to .env.local)
##############################################
#
# 1) Copy to project root as `.env.local`:
#    cp config/env.local.template .env.local
#
# 2) Fill in your real values.
#

# Session pooler (推奨: 通常のアプリ実行用)
DATABASE_URL="postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@YOUR_REGION.pooler.supabase.com:5432/postgres?sslmode=require"
# Direct connection (Prisma migrate用、必要なら)
# DIRECT_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres?sslmode=require"
# Prisma で direct を使うのは明示 opt-in のときだけ
# PARARIA_USE_DIRECT_DATABASE_URL=1
OPENAI_API_KEY="sk-proj-REPLACE_ME"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
