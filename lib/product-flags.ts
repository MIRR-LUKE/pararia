// The product contract is now interview-only. Lesson report runtime remains
// in the schema for backward compatibility, but the app must not expose it.
export const ENABLE_LESSON_REPORT_UI = false;
